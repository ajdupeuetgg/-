本文件夹内的Matlab代码来自于第二次数值计算。
感觉做得蛮辛苦并且效果还不错，于是上传。
main.m文件调用其他的文件中的所有函数，并有分支选择；
	注意cell转其他数据类型的方法；
window1.m文件中的脚本建立一个窗口，并接收用户输入的参数；
	注意它的返回是一个cell类型，需进行数据类型转换；
window2.select.m文件中的脚本建立一个窗口，并让用户单项选择某些选项，
当然可以多选，这取决于编程人员对于语言参数的设置；
mtd意为method，从mtdone到mtdfive是五个不同的迭代方法，大同小异。

